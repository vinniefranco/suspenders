# cargo-tarpaulin config (picked up automatically by `cargo tarpaulin`)
[default]
# llvm instrumentation is faster and more accurate than the ptrace default
engine = "Llvm"
# tests live under tests/unit/ (in-crate #[cfg(test)] #[path] submodules); don't count them as lines to cover
ignore-tests = true
out = ["Lcov", "Html"]
output-dir = "target/tarpaulin"
